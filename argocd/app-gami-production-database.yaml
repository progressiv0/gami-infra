apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gami-production-database
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/progressiv0/gami-infra
    targetRevision: main
    path: overlays/production/database
  destination:
    server: https://kubernetes.default.svc
    namespace: gami
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
