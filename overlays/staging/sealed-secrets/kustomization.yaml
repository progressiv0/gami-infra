apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Empty until an operator runs the Phase 3a bootstrap runbook against the
# staging cluster/namespace — independently-generated secrets from
# production, never copied over. Add each generated SealedSecret file here:
#   resources:
#     - gami-secrets.yaml
#     - gami-signing-key.yaml
#     - gami-smtp.yaml
resources: []
