apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Empty until an operator runs the Phase 3a bootstrap runbook against the
# production cluster. Add each generated SealedSecret file here as it's
# sealed — e.g.:
#   resources:
#     - gami-secrets.yaml
#     - gami-signing-key.yaml
#     - gami-smtp.yaml
resources: []
